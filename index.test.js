/* eslint-env mocha */

import should from 'should'
import sbp from '@sbp/sbp'
import './dist/module.mjs'

describe('[SBP] okTurtles.eventQueue domain', () => {
  it('should do something... TODO - this!', () => {
  })
})
